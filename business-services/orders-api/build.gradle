version = '0.0.1'

mainClassName = "com.epam.hw.netflix.OrdersApplication"

dependencies {
    compile project(':commons')
    compile libpacks.spring_common
    compile libpacks.cloud_common
    compile libpacks.cloud_config_client
    compile libpacks.groovy
    compile libpacks.logstash
    compile libpacks.json
    compile libpacks.spring_data_mongo
    compile libpacks.spring_kafka
}